using System;

namespace FluentAssertDemo
{
    public class CurrencyConverter
    {
        /// <summary>
        /// Convert a string describing a dollar amount to the equivalent string describing the amount in cents.
        /// </summary>
        /// <param name="dollars">The string describing the dollar amount, including the currency symbol.</param>
        /// <returns>The string describing the cents amount, including the currency symbol.</returns>
        public static string DollarsToCents(string dollars)
        {
            return "";
        }
    }
}
